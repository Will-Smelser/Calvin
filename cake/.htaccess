<IfModule mod_rewrite.c>
    Options -Multiviews
    Options +FollowSymLinks
    RewriteEngine On
    RewriteBase /

   RewriteEngine on
   RewriteRule    ^$ app/webroot/    [L]
   RewriteRule    (.*) app/webroot/$1 [L]
</IfModule>